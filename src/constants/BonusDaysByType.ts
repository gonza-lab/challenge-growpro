// Bonus days es la cantidad de días que valen un precio base
const BONUS_DAYS_BY_TYPE: { [name: string]: number } = {
  electric: 1,
  normal: 3,
  old: 5,
};

export default BONUS_DAYS_BY_TYPE;
